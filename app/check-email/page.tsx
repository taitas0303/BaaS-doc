export default function CheckEmail() {
    return (
      <div style={{ maxWidth: 400, margin: '0 auto', padding: '2rem' }}>
        <h1>メールを確認してください</h1>
        <p>登録はまだ完了していません。メールに記載されたリンクをクリックして認証を完了してください。</p>
      </div>
    );
  }